/**
 * Этот файл содержит функции, которые можно вызывать через API.
 */

function exampleFunction(name) {
    return { message: `Привет, ${name}! Это пример вызова функции.` };
}

function anotherFunction(a, b) {
    return a + b;
}
